CREATE TABLE tokens (
    id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    name TEXT NOT NULL,
    symbol TEXT NOT NULL,
    address TEXT NOT NULL UNIQUE,
    exchange_address TEXT NOT NULL UNIQUE
);
