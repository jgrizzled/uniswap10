CREATE TABLE tokens (
    id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    name TEXT NOT NULL,
    symbol TEXT NOT NULL UNIQUE,
    address TEXT NOT NULL UNIQUE,
    exchange_addresses TEXT[] NOT NULL
);
